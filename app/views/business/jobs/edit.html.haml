%h1 Editing business_job

= simple_form_for @job, url: business_job_path(@job) do |f|
  = f.error_notification
  = f.input :position
  = f.input :description
  = f.button :submit

= link_to 'Show', @business_job
\|
= link_to 'Back', business_jobs_path

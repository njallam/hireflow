%h2 Edit Profile
= simple_form_for @business, url: business_profile_path do |f|
  = f.error_notification
  = f.input :name
  = f.input :logo
  = f.input :city
  = f.input :size, label: 'Employees'
  = f.input :description
  = f.button :submit, 'Update Profile', class: 'btn btn-primary'

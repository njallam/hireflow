%h2 Your Applications
.row
  .col-md-12
    %h4 Action Required
    .card.bg-dark
      - if @action.any?
        - @action.each do |application|
          = render application
      - else
        .card-body
          %p.card-text No applications.
    %hr
    %h4 Waiting
    .card.bg-dark
      - if @waiting.any?
        - @waiting.each do |application|
          = render application
      - else
        .card-body
          %p.card-text No applications.
    %hr
    %h4 Completed
    .card.bg-dark
      - if @completed.any?
        - @completed.each do |application|
          = render application
      - else
        .card-body
          %p.card-text No applications.
